_schema-version: "3.2"
ID: nryzyappempleados
description: Generated by Fiori Tools
version: 0.0.1
modules:
- name: nryzyappempleados-destination-content
  type: com.sap.application.content
  requires:
  - name: nryzyappempleados-destination-service
    parameters:
      content-target: true
  - name: nryzyappempleados-repo-host
    parameters:
      service-key:
        name: nryzyappempleados-repo-host-key
  - name: nryzyappempleados-uaa
    parameters:
      service-key:
        name: nryzyappempleados-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: nryzyappempleados_html_repo_host
          ServiceInstanceName: nryzyappempleados-html5-srv
          ServiceKeyName: nryzyappempleados-repo-host-key
          sap.cloud.service: nryzyappempleados
        - Authentication: OAuth2UserTokenExchange
          Name: nryzyappempleados_uaa
          ServiceInstanceName: nryzyappempleados-xsuaa-srv
          ServiceKeyName: nryzyappempleados-uaa-key
          sap.cloud.service: nryzyappempleados
        existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: nryzyappempleados-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: nryzyappempleados-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - nryzyappempleados.zip
      name: nryzyappempleados
      target-path: resources/
- name: nryzyappempleados
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: nryzyappempleados-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: nryzyappempleados-destination-service
    service-plan: lite
- name: nryzyappempleados-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: nryzyappempleados-xsuaa-srv
    service-plan: application
- name: nryzyappempleados-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: nryzyappempleados-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
